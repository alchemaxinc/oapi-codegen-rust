// Code generated by oapi-codegen-rust. DO NOT EDIT.

#[derive(serde::Serialize, serde::Deserialize, Debug, Clone, PartialEq)]
pub struct Message {
    pub text: String,
}

/// Errors returned by the generated client.
#[derive(Debug)]
pub enum ClientError {
    /// The request failed to send, or the response body failed to decode.
    Http(reqwest::Error),
    /// The server returned a status code the operation does not declare.
    UnexpectedStatus(reqwest::StatusCode),
}
impl std::fmt::Display for ClientError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            ClientError::Http(error) => return write!(f, "HTTP request failed: {error}"),
            ClientError::UnexpectedStatus(status) => {
                return write!(f, "unexpected response status: {status}");
            }
        }
    }
}
impl std::error::Error for ClientError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            ClientError::Http(error) => return Some(error),
            ClientError::UnexpectedStatus(_) => return None,
        }
    }
}
impl From<reqwest::Error> for ClientError {
    fn from(error: reqwest::Error) -> Self {
        return ClientError::Http(error);
    }
}

/// Fetch the caller's profile, authenticated by the global bearer scheme.
pub enum GetProfileResponse {
    /// The caller's profile.
    Ok(Message),
}

/// Fetch a public resource that requires no authentication.
pub enum GetPublicResponse {
    /// The public resource.
    Ok(Message),
}

/// Fetch an admin resource, authenticated by HTTP basic auth.
pub enum GetAdminResponse {
    /// The admin resource.
    Ok(Message),
}

/// Fetch reports, authenticated by an API key header.
pub enum GetReportsResponse {
    /// The reports.
    Ok(Message),
}

#[derive(serde::Serialize, serde::Deserialize, Debug, Clone, PartialEq)]
pub struct SearchQuery {
    pub q: String,
}

/// Search, authenticated by an API key query parameter.
pub enum SearchResponse {
    /// The search results.
    Ok(Message),
}

/// Fetch session data, authenticated by an API key cookie.
pub enum GetSessionResponse {
    /// The session data.
    Ok(Message),
}

/// A blocking HTTP client for the API.
///
/// `base_url` is used as a prefix for every request path and should not
/// carry a trailing slash (e.g. `https://api.example.com`).
#[derive(Clone)]
pub struct Client {
    base_url: String,
    http: reqwest::blocking::Client,
    /// A JSON Web Token passed as a bearer credential.
    bearer_auth: Option<String>,
    /// HTTP basic username and password.
    basic_auth: Option<(String, String)>,
    /// API key sent in a request header.
    api_key_header: Option<String>,
    /// API key sent as a query parameter.
    api_key_query: Option<String>,
    /// API key sent as a cookie.
    api_key_cookie: Option<String>,
}

impl Client {
    /// Build a client targeting `base_url` with a default blocking
    /// `reqwest::blocking::Client`.
    pub fn new(base_url: impl Into<String>) -> Result<Self, ClientError> {
        let http = reqwest::blocking::Client::builder().build()?;
        return Ok(Self {
            base_url: base_url.into(),
            http,
            bearer_auth: None,
            basic_auth: None,
            api_key_header: None,
            api_key_query: None,
            api_key_cookie: None,
        });
    }
    /// Build a client targeting `base_url` with a caller-provided
    /// `reqwest::blocking::Client` (e.g. preconfigured with timeouts).
    pub fn with_client(
        base_url: impl Into<String>,
        http: reqwest::blocking::Client,
    ) -> Self {
        return Self {
            base_url: base_url.into(),
            http,
            bearer_auth: None,
            basic_auth: None,
            api_key_header: None,
            api_key_query: None,
            api_key_cookie: None,
        };
    }
    /// Set the credential for the `bearerAuth` security scheme.
    pub fn with_bearer_auth(mut self, credential: impl Into<String>) -> Self {
        self.bearer_auth = Some(credential.into());
        return self;
    }
    /// Set the username and password for the `basicAuth` HTTP basic scheme.
    pub fn with_basic_auth(
        mut self,
        username: impl Into<String>,
        password: impl Into<String>,
    ) -> Self {
        self.basic_auth = Some((username.into(), password.into()));
        return self;
    }
    /// Set the credential for the `apiKeyHeader` security scheme.
    pub fn with_api_key_header(mut self, credential: impl Into<String>) -> Self {
        self.api_key_header = Some(credential.into());
        return self;
    }
    /// Set the credential for the `apiKeyQuery` security scheme.
    pub fn with_api_key_query(mut self, credential: impl Into<String>) -> Self {
        self.api_key_query = Some(credential.into());
        return self;
    }
    /// Set the credential for the `apiKeyCookie` security scheme.
    pub fn with_api_key_cookie(mut self, credential: impl Into<String>) -> Self {
        self.api_key_cookie = Some(credential.into());
        return self;
    }
    /// Fetch the caller's profile, authenticated by the global bearer scheme.
    pub fn get_profile(&self) -> Result<GetProfileResponse, ClientError> {
        let url = format!("{}/profile", self.base_url);
        let mut request = self.http.request(reqwest::Method::GET, url);
        if let Some(token) = &self.bearer_auth {
            request = request.bearer_auth(token);
        }
        let response = request.send()?;
        let status = response.status();
        if status.as_u16() == 200 {
            let body: Message = response.json()?;
            return Ok(GetProfileResponse::Ok(body));
        }
        return Err(ClientError::UnexpectedStatus(status));
    }
    /// Fetch a public resource that requires no authentication.
    pub fn get_public(&self) -> Result<GetPublicResponse, ClientError> {
        let url = format!("{}/public", self.base_url);
        let response = self.http.request(reqwest::Method::GET, url).send()?;
        let status = response.status();
        if status.as_u16() == 200 {
            let body: Message = response.json()?;
            return Ok(GetPublicResponse::Ok(body));
        }
        return Err(ClientError::UnexpectedStatus(status));
    }
    /// Fetch an admin resource, authenticated by HTTP basic auth.
    pub fn get_admin(&self) -> Result<GetAdminResponse, ClientError> {
        let url = format!("{}/admin", self.base_url);
        let mut request = self.http.request(reqwest::Method::GET, url);
        if let Some((username, password)) = &self.basic_auth {
            request = request.basic_auth(username, Some(password));
        }
        let response = request.send()?;
        let status = response.status();
        if status.as_u16() == 200 {
            let body: Message = response.json()?;
            return Ok(GetAdminResponse::Ok(body));
        }
        return Err(ClientError::UnexpectedStatus(status));
    }
    /// Fetch reports, authenticated by an API key header.
    pub fn get_reports(&self) -> Result<GetReportsResponse, ClientError> {
        let url = format!("{}/reports", self.base_url);
        let mut request = self.http.request(reqwest::Method::GET, url);
        if let Some(value) = &self.api_key_header {
            request = request.header("X-API-Key", value.as_str());
        }
        let response = request.send()?;
        let status = response.status();
        if status.as_u16() == 200 {
            let body: Message = response.json()?;
            return Ok(GetReportsResponse::Ok(body));
        }
        return Err(ClientError::UnexpectedStatus(status));
    }
    /// Search, authenticated by an API key query parameter.
    pub fn search(&self, query: SearchQuery) -> Result<SearchResponse, ClientError> {
        let url = format!("{}/search", self.base_url);
        let mut request = self.http.request(reqwest::Method::GET, url);
        request = request.query(&[("q", query.q.to_string())]);
        if let Some(value) = &self.api_key_query {
            request = request.query(&[("api_key", value.as_str())]);
        }
        let response = request.send()?;
        let status = response.status();
        if status.as_u16() == 200 {
            let body: Message = response.json()?;
            return Ok(SearchResponse::Ok(body));
        }
        return Err(ClientError::UnexpectedStatus(status));
    }
    /// Fetch session data, authenticated by an API key cookie.
    pub fn get_session(&self) -> Result<GetSessionResponse, ClientError> {
        let url = format!("{}/session", self.base_url);
        let mut request = self.http.request(reqwest::Method::GET, url);
        let cookie_pairs: Vec<String> = [
            self.api_key_cookie.as_ref().map(|value| format!("SESSION={value}")),
        ]
            .into_iter()
            .flatten()
            .collect();
        if !cookie_pairs.is_empty() {
            request = request.header(reqwest::header::COOKIE, cookie_pairs.join("; "));
        }
        let response = request.send()?;
        let status = response.status();
        if status.as_u16() == 200 {
            let body: Message = response.json()?;
            return Ok(GetSessionResponse::Ok(body));
        }
        return Err(ClientError::UnexpectedStatus(status));
    }
}
