// Code generated by oapi-codegen-rust. DO NOT EDIT.
#![allow(
    dead_code,
    unused_imports,
    clippy::all,
    clippy::pedantic,
    clippy::nursery,
    clippy::restriction,
    reason = "generated code, not first-party source"
)]

/// An account, used as a request body and as a response body.
///
/// The request shape of `Account`. A `readOnly` property is not part of it.
#[derive(serde::Serialize, serde::Deserialize, Debug, Clone, PartialEq)]
pub struct AccountRequest {
    pub email: String,
    /// Sent when the account is created, and never read back.
    pub password: String,
}

/// An account, used as a request body and as a response body.
///
/// The response shape of `Account`. A `writeOnly` property is not part of it.
#[derive(serde::Serialize, serde::Deserialize, Debug, Clone, PartialEq)]
pub struct AccountResponse {
    /// Assigned by the server, so only a response carries it.
    pub id: String,
    pub email: String,
}

/// A response-only holder of a split model.
///
/// The response shape of `AccountPage`. A `writeOnly` property is not part of it.
#[derive(serde::Serialize, serde::Deserialize, Debug, Clone, PartialEq)]
pub struct AccountPageResponse {
    pub items: Vec<AccountResponse>,
}

/// List every account.
#[derive(Debug, Clone, PartialEq)]
pub enum ListAccountsResponse {
    /// The accounts, wrapped in a page.
    Ok(AccountPageResponse),
}

/// Create an account.
#[derive(Debug, Clone, PartialEq)]
pub enum CreateAccountResponse {
    /// The created account.
    Created(AccountResponse),
}

#[derive(Debug, Clone)]
pub struct UploadAvatarMultipart {
    pub image: Vec<u8>,
}

/// Replace an account avatar.
#[derive(Debug, Clone, PartialEq)]
pub enum UploadAvatarResponse {
    /// The avatar was stored.
    NoContent,
}

impl<S> axum::extract::FromRequest<S> for UploadAvatarMultipart
where
    S: Send + Sync,
{
    type Rejection = (axum::http::StatusCode, String);
    async fn from_request(
        request: axum::extract::Request,
        state: &S,
    ) -> Result<Self, Self::Rejection> {
        let mut multipart = <axum::extract::Multipart as axum::extract::FromRequest<
            S,
        >>::from_request(request, state)
            .await
            .map_err(|error| {
                return (axum::http::StatusCode::BAD_REQUEST, error.to_string());
            })?;
        let mut image: Option<Vec<u8>> = None;
        while let Some(field) = multipart
            .next_field()
            .await
            .map_err(|error| {
                return (axum::http::StatusCode::BAD_REQUEST, error.to_string());
            })?
        {
            let field_name = field.name().map(|name| return name.to_owned());
            match field_name.as_deref() {
                Some("image") => {
                    let value = field
                        .bytes()
                        .await
                        .map_err(|error| {
                            return (
                                axum::http::StatusCode::BAD_REQUEST,
                                error.to_string(),
                            );
                        })?;
                    image = Some(value.to_vec());
                }
                _ => {}
            }
        }
        return Ok(Self {
            image: image
                .ok_or((
                    axum::http::StatusCode::BAD_REQUEST,
                    "missing required multipart field `image`".to_owned(),
                ))?,
        });
    }
}

/// Server behaviour: implement one method per operation.
pub trait Api: Clone + Send + Sync + 'static {
    /// List every account.
    fn list_accounts(
        &self,
    ) -> impl std::future::Future<Output = ListAccountsResponse> + Send;
    /// Create an account.
    fn create_account(
        &self,
        body: AccountRequest,
    ) -> impl std::future::Future<Output = CreateAccountResponse> + Send;
    /// Replace an account avatar.
    fn upload_avatar(
        &self,
        account_id: String,
        body: UploadAvatarMultipart,
    ) -> impl std::future::Future<Output = UploadAvatarResponse> + Send;
}

impl axum::response::IntoResponse for ListAccountsResponse {
    fn into_response(self) -> axum::response::Response {
        match self {
            ListAccountsResponse::Ok(body) => {
                const STATUS: axum::http::StatusCode = match axum::http::StatusCode::from_u16(
                    200,
                ) {
                    Ok(status) => status,
                    Err(_) => panic!("oapi-codegen emitted an invalid HTTP status code"),
                };
                (STATUS, axum::Json(body)).into_response()
            }
        }
    }
}

impl axum::response::IntoResponse for CreateAccountResponse {
    fn into_response(self) -> axum::response::Response {
        match self {
            CreateAccountResponse::Created(body) => {
                const STATUS: axum::http::StatusCode = match axum::http::StatusCode::from_u16(
                    201,
                ) {
                    Ok(status) => status,
                    Err(_) => panic!("oapi-codegen emitted an invalid HTTP status code"),
                };
                (STATUS, axum::Json(body)).into_response()
            }
        }
    }
}

impl axum::response::IntoResponse for UploadAvatarResponse {
    fn into_response(self) -> axum::response::Response {
        match self {
            UploadAvatarResponse::NoContent => {
                const STATUS: axum::http::StatusCode = match axum::http::StatusCode::from_u16(
                    204,
                ) {
                    Ok(status) => status,
                    Err(_) => panic!("oapi-codegen emitted an invalid HTTP status code"),
                };
                STATUS.into_response()
            }
        }
    }
}

/// Build an axum `Router` that dispatches each route to `api`.
pub fn router<T: Api>(api: T) -> axum::Router {
    axum::Router::new()
        .route(
            "/accounts",
            axum::routing::get(list_accounts_handler::<T>)
                .post(create_account_handler::<T>),
        )
        .route(
            "/accounts/{accountId}/avatar",
            axum::routing::put(upload_avatar_handler::<T>),
        )
        .with_state(api)
}

async fn list_accounts_handler<T: Api>(
    axum::extract::State(api): axum::extract::State<T>,
) -> ListAccountsResponse {
    api.list_accounts().await
}

async fn create_account_handler<T: Api>(
    axum::extract::State(api): axum::extract::State<T>,
    axum::Json(body): axum::Json<AccountRequest>,
) -> CreateAccountResponse {
    api.create_account(body).await
}

async fn upload_avatar_handler<T: Api>(
    axum::extract::State(api): axum::extract::State<T>,
    axum::extract::Path(account_id): axum::extract::Path<String>,
    body: UploadAvatarMultipart,
) -> UploadAvatarResponse {
    api.upload_avatar(account_id, body).await
}

/// Errors returned by the generated client.
#[derive(Debug)]
pub enum ClientError {
    /// The `reqwest` request failed to send or complete, including any
    /// body decoding `reqwest` performs internally (such as JSON).
    Http(reqwest::Error),
    /// The server returned a status code the operation does not declare.
    UnexpectedStatus(reqwest::StatusCode),
    /// The response `Content-Type` matched none of the representations the
    /// operation declares for its status.
    UnexpectedContentType(String),
    /// The response cannot be decoded: a body that failed to
    /// deserialize (for example malformed form-urlencoded content), or a
    /// required response header that was missing or unparsable.
    Decode(String),
}
impl std::fmt::Display for ClientError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            ClientError::Http(error) => return write!(f, "HTTP request failed: {error}"),
            ClientError::UnexpectedStatus(status) => {
                return write!(f, "unexpected response status: {status}");
            }
            ClientError::UnexpectedContentType(content_type) => {
                return write!(f, "unexpected response content type: {content_type}");
            }
            ClientError::Decode(message) => {
                return write!(f, "failed to decode response: {message}");
            }
        }
    }
}
impl std::error::Error for ClientError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            ClientError::Http(error) => return Some(error),
            ClientError::UnexpectedStatus(_)
            | ClientError::UnexpectedContentType(_)
            | ClientError::Decode(_) => return None,
        }
    }
}
impl From<reqwest::Error> for ClientError {
    fn from(error: reqwest::Error) -> Self {
        return ClientError::Http(error);
    }
}

const PATH_PARAM_ENCODE_SET: &percent_encoding::AsciiSet = &percent_encoding::NON_ALPHANUMERIC
    .remove(b'-')
    .remove(b'.')
    .remove(b'_')
    .remove(b'~');

/// A blocking HTTP client for the API.
///
/// `base_url` is used as a prefix for every request path and must not
/// carry a trailing slash (for example `https://api.example.com`).
#[derive(Debug, Clone)]
pub struct Client {
    base_url: String,
    http: reqwest::blocking::Client,
}

impl Client {
    /// Build a client targeting `base_url` with a default blocking
    /// `reqwest::blocking::Client`.
    pub fn new(base_url: impl Into<String>) -> Result<Self, ClientError> {
        let http = reqwest::blocking::Client::builder().build()?;
        return Ok(Self {
            base_url: base_url.into(),
            http,
        });
    }
    /// Build a client targeting `base_url` with a caller-provided
    /// `reqwest::blocking::Client` (for example preconfigured with timeouts).
    pub fn with_client(
        base_url: impl Into<String>,
        http: reqwest::blocking::Client,
    ) -> Self {
        return Self {
            base_url: base_url.into(),
            http,
        };
    }
    /// List every account.
    pub fn list_accounts(&self) -> Result<ListAccountsResponse, ClientError> {
        let url = format!("{}/accounts", self.base_url);
        let response = self.http.request(reqwest::Method::GET, url).send()?;
        let status = response.status();
        if status.as_u16() == 200 {
            let body: AccountPageResponse = response.json()?;
            return Ok(ListAccountsResponse::Ok(body));
        }
        return Err(ClientError::UnexpectedStatus(status));
    }
    /// Create an account.
    pub fn create_account(
        &self,
        body: AccountRequest,
    ) -> Result<CreateAccountResponse, ClientError> {
        let url = format!("{}/accounts", self.base_url);
        let mut request = self.http.request(reqwest::Method::POST, url);
        request = request.json(&body);
        let response = request.send()?;
        let status = response.status();
        if status.as_u16() == 201 {
            let body: AccountResponse = response.json()?;
            return Ok(CreateAccountResponse::Created(body));
        }
        return Err(ClientError::UnexpectedStatus(status));
    }
    /// Replace an account avatar.
    pub fn upload_avatar(
        &self,
        account_id: String,
        body: UploadAvatarMultipart,
    ) -> Result<UploadAvatarResponse, ClientError> {
        let url = format!(
            "{}/accounts/{}/avatar", self.base_url,
            percent_encoding::utf8_percent_encode(account_id.as_str(),
            PATH_PARAM_ENCODE_SET)
        );
        let mut request = self.http.request(reqwest::Method::PUT, url);
        let mut form = reqwest::blocking::multipart::Form::new();
        form = form
            .part(
                "image",
                reqwest::blocking::multipart::Part::bytes(body.image).file_name("image"),
            );
        request = request.multipart(form);
        let response = request.send()?;
        let status = response.status();
        if status.as_u16() == 204 {
            return Ok(UploadAvatarResponse::NoContent);
        }
        return Err(ClientError::UnexpectedStatus(status));
    }
}
