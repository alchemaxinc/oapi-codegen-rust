// Code generated by oapi-codegen-rust. DO NOT EDIT.
#![allow(
    dead_code,
    unused_imports,
    clippy::all,
    clippy::pedantic,
    clippy::nursery,
    clippy::restriction,
    reason = "generated code, not first-party source"
)]

#[derive(serde::Serialize, Debug, Clone, PartialEq)]
pub struct Report {
    pub title: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub lines: Option<i64>,
}

#[derive(Debug, Clone, PartialEq)]
pub enum GetReportResponseOkBody {
    Json(Report),
    Text(String),
}

/// Fetch the report as JSON or plain text (handler chooses).
#[derive(Debug, Clone, PartialEq)]
pub enum GetReportResponse {
    /// The report, as JSON or plain text.
    Ok(GetReportResponseOkBody),
}

#[derive(Debug, Clone, PartialEq)]
pub enum ExportReportResponseDefaultBody {
    Json(Report),
    Text(String),
}

/// Export the report; the handler also sets the status code.
#[derive(Debug, Clone, PartialEq)]
pub enum ExportReportResponse {
    /// The report, as JSON or plain text.
    Default(http::StatusCode, ExportReportResponseDefaultBody),
}

#[derive(Debug, Clone, PartialEq)]
pub enum GetSummaryResponseOkBody {
    Json(Report),
    Text(String),
}

/// Fetch the summary as JSON or plain text, with a header.
#[derive(Debug, Clone, PartialEq)]
pub enum GetSummaryResponse {
    /// The summary, as JSON or plain text.
    Ok { body: GetSummaryResponseOkBody, x_report_id: Option<String> },
}

/// Server behaviour: implement one method per operation.
pub trait Api: Clone + Send + Sync + 'static {
    /// Fetch the report as JSON or plain text (handler chooses).
    fn get_report(&self) -> impl std::future::Future<Output = GetReportResponse> + Send;
    /// Export the report; the handler also sets the status code.
    fn export_report(
        &self,
    ) -> impl std::future::Future<Output = ExportReportResponse> + Send;
    /// Fetch the summary as JSON or plain text, with a header.
    fn get_summary(
        &self,
    ) -> impl std::future::Future<Output = GetSummaryResponse> + Send;
}

impl axum::response::IntoResponse for GetReportResponse {
    fn into_response(self) -> axum::response::Response {
        match self {
            GetReportResponse::Ok(body) => {
                const STATUS: axum::http::StatusCode = match axum::http::StatusCode::from_u16(
                    200,
                ) {
                    Ok(status) => status,
                    Err(_) => panic!("oapi-codegen emitted an invalid HTTP status code"),
                };
                match body {
                    GetReportResponseOkBody::Json(body) => {
                        (STATUS, axum::Json(body)).into_response()
                    }
                    GetReportResponseOkBody::Text(body) => (STATUS, body).into_response(),
                }
            }
        }
    }
}

impl axum::response::IntoResponse for ExportReportResponse {
    fn into_response(self) -> axum::response::Response {
        match self {
            ExportReportResponse::Default(status, body) => {
                match body {
                    ExportReportResponseDefaultBody::Json(body) => {
                        (status, axum::Json(body)).into_response()
                    }
                    ExportReportResponseDefaultBody::Text(body) => {
                        (status, body).into_response()
                    }
                }
            }
        }
    }
}

impl axum::response::IntoResponse for GetSummaryResponse {
    fn into_response(self) -> axum::response::Response {
        match self {
            GetSummaryResponse::Ok { body, x_report_id } => {
                let mut header_map = axum::http::HeaderMap::new();
                if let Some(x_report_id) = x_report_id {
                    if let Ok(value) = axum::http::HeaderValue::from_str(
                        &x_report_id.to_string(),
                    ) {
                        header_map
                            .insert(
                                axum::http::HeaderName::from_static("x-report-id"),
                                value,
                            );
                    }
                }
                let response_status = {
                    const STATUS: axum::http::StatusCode = match axum::http::StatusCode::from_u16(
                        200,
                    ) {
                        Ok(status) => status,
                        Err(_) => {
                            panic!("oapi-codegen emitted an invalid HTTP status code")
                        }
                    };
                    STATUS
                };
                match body {
                    GetSummaryResponseOkBody::Json(body) => {
                        (response_status, header_map, axum::Json(body)).into_response()
                    }
                    GetSummaryResponseOkBody::Text(body) => {
                        (response_status, header_map, body).into_response()
                    }
                }
            }
        }
    }
}

/// Build an axum `Router` that dispatches each route to `api`.
pub fn router<T: Api>(api: T) -> axum::Router {
    axum::Router::new()
        .route("/report", axum::routing::get(get_report_handler::<T>))
        .route("/export", axum::routing::get(export_report_handler::<T>))
        .route("/summary", axum::routing::get(get_summary_handler::<T>))
        .with_state(api)
}

async fn get_report_handler<T: Api>(
    axum::extract::State(api): axum::extract::State<T>,
) -> GetReportResponse {
    api.get_report().await
}

async fn export_report_handler<T: Api>(
    axum::extract::State(api): axum::extract::State<T>,
) -> ExportReportResponse {
    api.export_report().await
}

async fn get_summary_handler<T: Api>(
    axum::extract::State(api): axum::extract::State<T>,
) -> GetSummaryResponse {
    api.get_summary().await
}
