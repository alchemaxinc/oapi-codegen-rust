// Code generated by oapi-codegen-rust. DO NOT EDIT.
#![allow(
    dead_code,
    unused_imports,
    clippy::all,
    clippy::pedantic,
    clippy::nursery,
    clippy::restriction,
    reason = "generated code, not first-party source"
)]

#[derive(serde::Serialize, Debug, Clone, PartialEq)]
pub struct Message {
    pub text: String,
}

/// Fetch the caller's profile, which the global bearer scheme protects.
#[derive(Debug, Clone, PartialEq)]
pub enum GetProfileResponse {
    /// The caller's profile.
    Ok(Message),
}

/// Fetch a public resource, which needs no credential.
#[derive(Debug, Clone, PartialEq)]
pub enum GetPublicResponse {
    /// The public resource.
    Ok(Message),
}

#[derive(Debug, Clone, PartialEq)]
pub enum GetReportsResponse {
    /// The reports.
    Ok(Message),
}

/// Fetch session data, which an API key cookie protects.
///
/// The cookie is set at login and expires with the session.
#[derive(Debug, Clone, PartialEq)]
pub enum GetSessionResponse {
    /// The session data.
    Ok(Message),
}

/// Search, which an API key query parameter protects.
#[derive(Debug, Clone, PartialEq)]
pub enum SearchResponse {
    /// The search results.
    Ok(Message),
}

/// Fetch the audit log, which OAuth2 protects.
#[derive(Debug, Clone, PartialEq)]
pub enum GetAuditResponse {
    /// The audit log.
    Ok(Message),
}

/// Server behaviour: implement one method per operation.
pub trait Api: Clone + Send + Sync + 'static {
    /// Fetch the caller's profile, which the global bearer scheme protects.
    ///
    /// # Security
    ///
    /// The document names these security schemes for this operation:
    ///
    /// - `bearerAuth`: a bearer token in the `Authorization` header
    ///
    /// This generator emits no check. Enforce it in a layer around the router: this method receives only the parameters the operation declares, not the credential.
    fn get_profile(
        &self,
    ) -> impl std::future::Future<Output = GetProfileResponse> + Send;
    /// Fetch a public resource, which needs no credential.
    fn get_public(&self) -> impl std::future::Future<Output = GetPublicResponse> + Send;
    /// # Security
    ///
    /// The document names these security schemes for this operation:
    ///
    /// - `apiKeyHeader`: an API key in the `X-API-Key` header
    /// - `basicAuth`: basic credentials in the `Authorization` header
    ///
    /// This list is the union of every alternative the document gives, so it may be a choice between schemes rather than all of them. Read `security` in the document for the exact rule.
    ///
    /// This generator emits no check. Enforce it in a layer around the router: this method receives only the parameters the operation declares, not the credential.
    fn get_reports(
        &self,
    ) -> impl std::future::Future<Output = GetReportsResponse> + Send;
    /// Fetch session data, which an API key cookie protects.
    ///
    /// The cookie is set at login and expires with the session.
    ///
    /// # Security
    ///
    /// The document names these security schemes for this operation:
    ///
    /// - `apiKeyCookie`: an API key in the `SESSION` cookie
    ///
    /// This generator emits no check. Enforce it in a layer around the router: this method receives only the parameters the operation declares, not the credential.
    fn get_session(
        &self,
    ) -> impl std::future::Future<Output = GetSessionResponse> + Send;
    /// Search, which an API key query parameter protects.
    ///
    /// # Security
    ///
    /// The document names these security schemes for this operation:
    ///
    /// - `apiKeyQuery`: an API key in the `api_key` query parameter
    ///
    /// This generator emits no check. Enforce it in a layer around the router: this method receives only the parameters the operation declares, not the credential.
    fn search(&self) -> impl std::future::Future<Output = SearchResponse> + Send;
    /// Fetch the audit log, which OAuth2 protects.
    ///
    /// # Security
    ///
    /// The document names these security schemes for this operation:
    ///
    /// - `oauth2`, a scheme this generator has no built-in support for
    ///
    /// This generator emits no check. Enforce it in a layer around the router: this method receives only the parameters the operation declares, not the credential.
    fn get_audit(&self) -> impl std::future::Future<Output = GetAuditResponse> + Send;
}

impl axum::response::IntoResponse for GetProfileResponse {
    fn into_response(self) -> axum::response::Response {
        match self {
            GetProfileResponse::Ok(body) => {
                const STATUS: axum::http::StatusCode = match axum::http::StatusCode::from_u16(
                    200,
                ) {
                    Ok(status) => status,
                    Err(_) => panic!("oapi-codegen emitted an invalid HTTP status code"),
                };
                (STATUS, axum::Json(body)).into_response()
            }
        }
    }
}

impl axum::response::IntoResponse for GetPublicResponse {
    fn into_response(self) -> axum::response::Response {
        match self {
            GetPublicResponse::Ok(body) => {
                const STATUS: axum::http::StatusCode = match axum::http::StatusCode::from_u16(
                    200,
                ) {
                    Ok(status) => status,
                    Err(_) => panic!("oapi-codegen emitted an invalid HTTP status code"),
                };
                (STATUS, axum::Json(body)).into_response()
            }
        }
    }
}

impl axum::response::IntoResponse for GetReportsResponse {
    fn into_response(self) -> axum::response::Response {
        match self {
            GetReportsResponse::Ok(body) => {
                const STATUS: axum::http::StatusCode = match axum::http::StatusCode::from_u16(
                    200,
                ) {
                    Ok(status) => status,
                    Err(_) => panic!("oapi-codegen emitted an invalid HTTP status code"),
                };
                (STATUS, axum::Json(body)).into_response()
            }
        }
    }
}

impl axum::response::IntoResponse for GetSessionResponse {
    fn into_response(self) -> axum::response::Response {
        match self {
            GetSessionResponse::Ok(body) => {
                const STATUS: axum::http::StatusCode = match axum::http::StatusCode::from_u16(
                    200,
                ) {
                    Ok(status) => status,
                    Err(_) => panic!("oapi-codegen emitted an invalid HTTP status code"),
                };
                (STATUS, axum::Json(body)).into_response()
            }
        }
    }
}

impl axum::response::IntoResponse for SearchResponse {
    fn into_response(self) -> axum::response::Response {
        match self {
            SearchResponse::Ok(body) => {
                const STATUS: axum::http::StatusCode = match axum::http::StatusCode::from_u16(
                    200,
                ) {
                    Ok(status) => status,
                    Err(_) => panic!("oapi-codegen emitted an invalid HTTP status code"),
                };
                (STATUS, axum::Json(body)).into_response()
            }
        }
    }
}

impl axum::response::IntoResponse for GetAuditResponse {
    fn into_response(self) -> axum::response::Response {
        match self {
            GetAuditResponse::Ok(body) => {
                const STATUS: axum::http::StatusCode = match axum::http::StatusCode::from_u16(
                    200,
                ) {
                    Ok(status) => status,
                    Err(_) => panic!("oapi-codegen emitted an invalid HTTP status code"),
                };
                (STATUS, axum::Json(body)).into_response()
            }
        }
    }
}

/// Build an axum `Router` that dispatches each route to `api`.
pub fn router<T: Api>(api: T) -> axum::Router {
    axum::Router::new()
        .route("/profile", axum::routing::get(get_profile_handler::<T>))
        .route("/public", axum::routing::get(get_public_handler::<T>))
        .route("/reports", axum::routing::get(get_reports_handler::<T>))
        .route("/session", axum::routing::get(get_session_handler::<T>))
        .route("/search", axum::routing::get(search_handler::<T>))
        .route("/audit", axum::routing::get(get_audit_handler::<T>))
        .with_state(api)
}

async fn get_profile_handler<T: Api>(
    axum::extract::State(api): axum::extract::State<T>,
) -> GetProfileResponse {
    api.get_profile().await
}

async fn get_public_handler<T: Api>(
    axum::extract::State(api): axum::extract::State<T>,
) -> GetPublicResponse {
    api.get_public().await
}

async fn get_reports_handler<T: Api>(
    axum::extract::State(api): axum::extract::State<T>,
) -> GetReportsResponse {
    api.get_reports().await
}

async fn get_session_handler<T: Api>(
    axum::extract::State(api): axum::extract::State<T>,
) -> GetSessionResponse {
    api.get_session().await
}

async fn search_handler<T: Api>(
    axum::extract::State(api): axum::extract::State<T>,
) -> SearchResponse {
    api.search().await
}

async fn get_audit_handler<T: Api>(
    axum::extract::State(api): axum::extract::State<T>,
) -> GetAuditResponse {
    api.get_audit().await
}
