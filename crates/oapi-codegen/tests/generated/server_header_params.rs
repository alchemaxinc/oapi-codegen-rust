// Code generated by oapi-codegen-rust. DO NOT EDIT.
#![allow(
    dead_code,
    clippy::all,
    clippy::pedantic,
    clippy::nursery,
    clippy::restriction,
    reason = "generated code, not first-party source"
)]

#[derive(Debug, Clone)]
pub struct GetWidgetsHeaders {
    /// Tenant that owns the widgets.
    pub x_tenant: String,
    /// Correlation id echoed back in logs.
    pub x_request_id: String,
    /// Cap on the number of widgets returned.
    pub x_max_items: Option<i32>,
    pub x_debug: Option<bool>,
    pub x_locale: Option<String>,
}

/// List widgets using typed request headers.
#[derive(Debug, Clone, PartialEq)]
pub enum GetWidgetsResponse {
    /// The matching widgets.
    Ok,
}

impl<S> axum::extract::FromRequestParts<S> for GetWidgetsHeaders
where
    S: Send + Sync,
{
    type Rejection = (axum::http::StatusCode, String);
    async fn from_request_parts(
        parts: &mut axum::http::request::Parts,
        _state: &S,
    ) -> Result<Self, Self::Rejection> {
        let x_tenant = match parts.headers.get("X-Tenant") {
            Some(value) => {
                let text = match value.to_str() {
                    Ok(text) => text,
                    Err(_) => {
                        return Err((
                            axum::http::StatusCode::BAD_REQUEST,
                            "header `X-Tenant` is not valid text".to_owned(),
                        ));
                    }
                };
                text.to_owned()
            }
            None => {
                return Err((
                    axum::http::StatusCode::BAD_REQUEST,
                    "missing required header `X-Tenant`".to_owned(),
                ));
            }
        };
        let x_request_id = match parts.headers.get("X-Request-Id") {
            Some(value) => {
                let text = match value.to_str() {
                    Ok(text) => text,
                    Err(_) => {
                        return Err((
                            axum::http::StatusCode::BAD_REQUEST,
                            "header `X-Request-Id` is not valid text".to_owned(),
                        ));
                    }
                };
                text.to_owned()
            }
            None => {
                return Err((
                    axum::http::StatusCode::BAD_REQUEST,
                    "missing required header `X-Request-Id`".to_owned(),
                ));
            }
        };
        let x_max_items = match parts.headers.get("X-Max-Items") {
            Some(value) => {
                let text = match value.to_str() {
                    Ok(text) => text,
                    Err(_) => {
                        return Err((
                            axum::http::StatusCode::BAD_REQUEST,
                            "header `X-Max-Items` is not valid text".to_owned(),
                        ));
                    }
                };
                Some(
                    match text.trim().parse::<i32>() {
                        Ok(parsed) => parsed,
                        Err(_) => {
                            return Err((
                                axum::http::StatusCode::BAD_REQUEST,
                                "header `X-Max-Items` has an invalid value".to_owned(),
                            ));
                        }
                    },
                )
            }
            None => None,
        };
        let x_debug = match parts.headers.get("X-Debug") {
            Some(value) => {
                let text = match value.to_str() {
                    Ok(text) => text,
                    Err(_) => {
                        return Err((
                            axum::http::StatusCode::BAD_REQUEST,
                            "header `X-Debug` is not valid text".to_owned(),
                        ));
                    }
                };
                Some(
                    match text.trim().parse::<bool>() {
                        Ok(parsed) => parsed,
                        Err(_) => {
                            return Err((
                                axum::http::StatusCode::BAD_REQUEST,
                                "header `X-Debug` has an invalid value".to_owned(),
                            ));
                        }
                    },
                )
            }
            None => None,
        };
        let x_locale = match parts.headers.get("X-Locale") {
            Some(value) => {
                let text = match value.to_str() {
                    Ok(text) => text,
                    Err(_) => {
                        return Err((
                            axum::http::StatusCode::BAD_REQUEST,
                            "header `X-Locale` is not valid text".to_owned(),
                        ));
                    }
                };
                Some(text.to_owned())
            }
            None => None,
        };
        return Ok(Self {
            x_tenant,
            x_request_id,
            x_max_items,
            x_debug,
            x_locale,
        });
    }
}

/// Server behaviour: implement one method per operation.
pub trait Api: Clone + Send + Sync + 'static {
    /// List widgets using typed request headers.
    fn get_widgets(
        &self,
        headers: GetWidgetsHeaders,
    ) -> impl std::future::Future<Output = GetWidgetsResponse> + Send;
}

impl axum::response::IntoResponse for GetWidgetsResponse {
    fn into_response(self) -> axum::response::Response {
        match self {
            GetWidgetsResponse::Ok => {
                const STATUS: axum::http::StatusCode = match axum::http::StatusCode::from_u16(
                    200,
                ) {
                    Ok(status) => status,
                    Err(_) => panic!("oapi-codegen emitted an invalid HTTP status code"),
                };
                STATUS.into_response()
            }
        }
    }
}

/// Build an axum `Router` that dispatches each route to `api`.
pub fn router<T: Api>(api: T) -> axum::Router {
    axum::Router::new()
        .route("/widgets", axum::routing::get(get_widgets_handler::<T>))
        .with_state(api)
}

async fn get_widgets_handler<T: Api>(
    axum::extract::State(api): axum::extract::State<T>,
    headers: GetWidgetsHeaders,
) -> GetWidgetsResponse {
    api.get_widgets(headers).await
}
