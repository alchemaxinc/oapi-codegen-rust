// Code generated by oapi-codegen-rust. DO NOT EDIT.
#![allow(
    dead_code,
    unused_imports,
    clippy::all,
    clippy::pedantic,
    clippy::nursery,
    clippy::restriction,
    reason = "generated code, not first-party source"
)]

#[derive(serde::Serialize, serde::Deserialize, Debug, Clone, PartialEq)]
pub struct Widget {
    pub name: String,
}

#[derive(Debug, Clone, PartialEq)]
pub enum TypeResponse {
    /// A widget
    Ok(Widget),
}

#[derive(Debug, Clone, PartialEq)]
pub enum ModResponse {
    /// A widget
    Ok(Widget),
}

#[derive(Debug, Clone, PartialEq)]
pub enum SelfResponse {
    /// A widget
    Ok(Widget),
}

#[derive(Debug, Clone, PartialEq)]
pub enum MatchResponse {
    /// A widget
    Ok(Widget),
}

/// Server behaviour: implement one method per operation.
pub trait Api: Clone + Send + Sync + 'static {
    fn r#type(&self) -> impl std::future::Future<Output = TypeResponse> + Send;
    fn r#mod(&self) -> impl std::future::Future<Output = ModResponse> + Send;
    fn self_(&self) -> impl std::future::Future<Output = SelfResponse> + Send;
    fn r#match(
        &self,
        id: String,
        body: Widget,
    ) -> impl std::future::Future<Output = MatchResponse> + Send;
}

impl axum::response::IntoResponse for TypeResponse {
    fn into_response(self) -> axum::response::Response {
        match self {
            TypeResponse::Ok(body) => {
                const STATUS: axum::http::StatusCode = match axum::http::StatusCode::from_u16(
                    200,
                ) {
                    Ok(status) => status,
                    Err(_) => panic!("oapi-codegen emitted an invalid HTTP status code"),
                };
                (STATUS, axum::Json(body)).into_response()
            }
        }
    }
}

impl axum::response::IntoResponse for ModResponse {
    fn into_response(self) -> axum::response::Response {
        match self {
            ModResponse::Ok(body) => {
                const STATUS: axum::http::StatusCode = match axum::http::StatusCode::from_u16(
                    200,
                ) {
                    Ok(status) => status,
                    Err(_) => panic!("oapi-codegen emitted an invalid HTTP status code"),
                };
                (STATUS, axum::Json(body)).into_response()
            }
        }
    }
}

impl axum::response::IntoResponse for SelfResponse {
    fn into_response(self) -> axum::response::Response {
        match self {
            SelfResponse::Ok(body) => {
                const STATUS: axum::http::StatusCode = match axum::http::StatusCode::from_u16(
                    200,
                ) {
                    Ok(status) => status,
                    Err(_) => panic!("oapi-codegen emitted an invalid HTTP status code"),
                };
                (STATUS, axum::Json(body)).into_response()
            }
        }
    }
}

impl axum::response::IntoResponse for MatchResponse {
    fn into_response(self) -> axum::response::Response {
        match self {
            MatchResponse::Ok(body) => {
                const STATUS: axum::http::StatusCode = match axum::http::StatusCode::from_u16(
                    200,
                ) {
                    Ok(status) => status,
                    Err(_) => panic!("oapi-codegen emitted an invalid HTTP status code"),
                };
                (STATUS, axum::Json(body)).into_response()
            }
        }
    }
}

/// Build an axum `Router` that dispatches each route to `api`.
pub fn router<T: Api>(api: T) -> axum::Router {
    axum::Router::new()
        .route("/type", axum::routing::get(type_handler::<T>))
        .route("/mod", axum::routing::get(mod_handler::<T>))
        .route("/self", axum::routing::get(self_handler::<T>))
        .route("/match/{id}", axum::routing::post(match_handler::<T>))
        .with_state(api)
}

async fn type_handler<T: Api>(
    axum::extract::State(api): axum::extract::State<T>,
) -> TypeResponse {
    api.r#type().await
}

async fn mod_handler<T: Api>(
    axum::extract::State(api): axum::extract::State<T>,
) -> ModResponse {
    api.r#mod().await
}

async fn self_handler<T: Api>(
    axum::extract::State(api): axum::extract::State<T>,
) -> SelfResponse {
    api.self_().await
}

async fn match_handler<T: Api>(
    axum::extract::State(api): axum::extract::State<T>,
    axum::extract::Path(id): axum::extract::Path<String>,
    axum::Json(body): axum::Json<Widget>,
) -> MatchResponse {
    api.r#match(id, body).await
}

/// Errors returned by the generated client.
#[derive(Debug)]
pub enum ClientError {
    /// The `reqwest` request failed to send or complete, including any
    /// body decoding `reqwest` performs internally (such as JSON).
    Http(reqwest::Error),
    /// The server returned a status code the operation does not declare.
    UnexpectedStatus(reqwest::StatusCode),
    /// The response `Content-Type` matched none of the representations the
    /// operation declares for its status.
    UnexpectedContentType(String),
    /// The response cannot be decoded: a body that failed to
    /// deserialize (for example malformed form-urlencoded content), or a
    /// required response header that was missing or unparsable.
    Decode(String),
}
impl std::fmt::Display for ClientError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            ClientError::Http(error) => return write!(f, "HTTP request failed: {error}"),
            ClientError::UnexpectedStatus(status) => {
                return write!(f, "unexpected response status: {status}");
            }
            ClientError::UnexpectedContentType(content_type) => {
                return write!(f, "unexpected response content type: {content_type}");
            }
            ClientError::Decode(message) => {
                return write!(f, "failed to decode response: {message}");
            }
        }
    }
}
impl std::error::Error for ClientError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            ClientError::Http(error) => return Some(error),
            ClientError::UnexpectedStatus(_)
            | ClientError::UnexpectedContentType(_)
            | ClientError::Decode(_) => return None,
        }
    }
}
impl From<reqwest::Error> for ClientError {
    fn from(error: reqwest::Error) -> Self {
        return ClientError::Http(error);
    }
}

const PATH_PARAM_ENCODE_SET: &percent_encoding::AsciiSet = &percent_encoding::NON_ALPHANUMERIC
    .remove(b'-')
    .remove(b'.')
    .remove(b'_')
    .remove(b'~');

/// A blocking HTTP client for the API.
///
/// `base_url` is used as a prefix for every request path and must not
/// carry a trailing slash (for example `https://api.example.com`).
#[derive(Debug, Clone)]
pub struct Client {
    base_url: String,
    http: reqwest::blocking::Client,
}

impl Client {
    /// Build a client targeting `base_url` with a default blocking
    /// `reqwest::blocking::Client`.
    pub fn new(base_url: impl Into<String>) -> Result<Self, ClientError> {
        let http = reqwest::blocking::Client::builder().build()?;
        return Ok(Self {
            base_url: base_url.into(),
            http,
        });
    }
    /// Build a client targeting `base_url` with a caller-provided
    /// `reqwest::blocking::Client` (for example preconfigured with timeouts).
    pub fn with_client(
        base_url: impl Into<String>,
        http: reqwest::blocking::Client,
    ) -> Self {
        return Self {
            base_url: base_url.into(),
            http,
        };
    }
    pub fn r#type(&self) -> Result<TypeResponse, ClientError> {
        let url = format!("{}/type", self.base_url);
        let response = self.http.request(reqwest::Method::GET, url).send()?;
        let status = response.status();
        if status.as_u16() == 200 {
            let body: Widget = response.json()?;
            return Ok(TypeResponse::Ok(body));
        }
        return Err(ClientError::UnexpectedStatus(status));
    }
    pub fn r#mod(&self) -> Result<ModResponse, ClientError> {
        let url = format!("{}/mod", self.base_url);
        let response = self.http.request(reqwest::Method::GET, url).send()?;
        let status = response.status();
        if status.as_u16() == 200 {
            let body: Widget = response.json()?;
            return Ok(ModResponse::Ok(body));
        }
        return Err(ClientError::UnexpectedStatus(status));
    }
    pub fn self_(&self) -> Result<SelfResponse, ClientError> {
        let url = format!("{}/self", self.base_url);
        let response = self.http.request(reqwest::Method::GET, url).send()?;
        let status = response.status();
        if status.as_u16() == 200 {
            let body: Widget = response.json()?;
            return Ok(SelfResponse::Ok(body));
        }
        return Err(ClientError::UnexpectedStatus(status));
    }
    pub fn r#match(
        &self,
        id: String,
        body: Widget,
    ) -> Result<MatchResponse, ClientError> {
        let url = format!(
            "{}/match/{}", self.base_url, percent_encoding::utf8_percent_encode(id
            .as_str(), PATH_PARAM_ENCODE_SET)
        );
        let mut request = self.http.request(reqwest::Method::POST, url);
        request = request.json(&body);
        let response = request.send()?;
        let status = response.status();
        if status.as_u16() == 200 {
            let body: Widget = response.json()?;
            return Ok(MatchResponse::Ok(body));
        }
        return Err(ClientError::UnexpectedStatus(status));
    }
}
