openapi: 3.0.3
info:
  title: Vendor extensions
  version: 1.0.0
paths: {}
components:
  schemas:
    OriginalName:
      type: object
      x-rust-name: RenamedType
      required: [id]
      properties:
        id:
          type: string
    HasReference:
      type: object
      required: [item]
      properties:
        item:
          $ref: "#/components/schemas/OriginalName"
    Ordered:
      type: object
      properties:
        a_last:
          type: string
        b_first:
          type: string
          x-order: 1
        c_second:
          type: string
          x-order: 2
    ClientTypeVarnames:
      type: string
      enum: [ACT, EXP]
      x-enum-varnames: [Active, Expired]
    ClientTypeNames:
      type: string
      enum: [ACT, EXP]
      x-enumNames: [Active, Expired]
    Client:
      type: object
      deprecated: true
      x-deprecated-reason: use the v2 client
      required: [name]
      properties:
        name:
          type: string
          deprecated: true
          x-deprecated-reason: use full_name instead
        account_id:
          type: number
          x-rust-name: account_identifier
        keep_null:
          type: string
          x-omitempty: false
        internal:
          type: object
          x-rust-serde-skip: true
          properties:
            secret:
              type: string
