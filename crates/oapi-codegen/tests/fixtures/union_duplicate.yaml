openapi: "3.0.3"
info:
  title: a oneOf that holds one type twice
  version: "1.0.0"
paths: {}
components:
  schemas:
    Cat:
      type: object
      required: [meow]
      properties:
        meow:
          type: string
    Pet:
      oneOf:
        - $ref: "#/components/schemas/Cat"
        - $ref: "#/components/schemas/Cat"
