openapi: 3.1.0
info:
  title: a 3.1 document whose every construct also parses as 3.0
  version: "1.0.0"
paths: {}
components:
  schemas:
    Widget:
      type: object
      required: [id]
      properties:
        id:
          type: string
