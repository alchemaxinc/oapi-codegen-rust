[package]
name = "consumer"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8"
serde = { version = "1", features = ["derive"] }
http.workspace = true

[dev-dependencies]
reqwest = "0.13"

[target.'cfg(unix)'.dependencies]
uuid = "1"
