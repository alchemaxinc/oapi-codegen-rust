openapi: 3.0.3
info:
  title: unsupported bytes cookie param
  version: "1"
paths:
  /widgets:
    get:
      operationId: getWidgets
      parameters:
        - name: token
          in: cookie
          schema:
            type: string
            format: byte
      responses:
        "200":
          description: ok
