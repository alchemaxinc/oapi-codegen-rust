openapi: 3.0.3
info:
  title: response header via $ref (unsupported)
  version: "1"
paths:
  /widgets:
    get:
      operationId: getWidgets
      responses:
        "200":
          description: ok
          headers:
            X-Request-Id:
              $ref: "#/components/headers/RequestId"
          content:
            application/json:
              schema:
                type: string
components:
  headers:
    RequestId:
      required: true
      schema:
        type: string
