openapi: 3.0.3
info:
  title: Cross-file response ref server (referenced file missing)
  version: 1.0.0
paths:
  /widgets:
    get:
      operationId: listWidgets
      summary: List widgets.
      responses:
        "401":
          $ref: "schemas/common.yaml#/components/responses/Unauthorized"
