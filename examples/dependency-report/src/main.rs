//! This crate exists only to host the `README.md` demonstration of the
//! dependency report `oapi-codegen` prints after generating code. It
//! deliberately declares no HTTP dependencies, and does not compile the
//! generated file, so a generated server here reports the full set of crates a
//! consumer would need to add.
fn main() {}
